#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
    char s[100],s2[100];
    char c;
    fgets(s, 100, stdin); ;printf("%s\n",s);fgets(s, 100, stdin); ;printf("%s\n",s);
    fgets(s, 100, stdin); ;printf("%s\n",s);

    return 0;
}